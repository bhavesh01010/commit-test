## Frontend with Vite App

#### `cd frontend`

#### `npm install`

#### `npm run dev`

## Backend with Node.js

#### `cd server`

#### `make .env file and store MONGO_URI`

#### `npm install`

#### `npm start`
